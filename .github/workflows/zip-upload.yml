name: Upload ZIP to latest release

on:
  workflow_run:
    workflows: ["Build ZIP (auto)"]
    types: [completed]

permissions:
  contents: write

jobs:
  upload:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Upload ZIP to release from data/version.txt (tag already includes v)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail

          TAG="$(tr -d ' \r\n' < data/version.txt)"
          FILE="releases/zip/wildshape-toolkit-dnd5e-latest.zip"

          test -f "$FILE"

          # Ensure release for this tag exists
          gh release view "$TAG" >/dev/null 2>&1 || \
            gh release create "$TAG" --title "$TAG" --notes ""

          # Upload/overwrite asset into that release
          gh release upload "$TAG" "$FILE" --clobber
